---
import PlusIcon from "../icons/plus.astro";
import clsx from "clsx";

type Props = {
  availableForSale: boolean;
  selectedVariantId: string | undefined;
};

const buttonClasses =
  "relative flex w-full items-center justify-center rounded-full bg-[#171615] p-4 font-bold text-white shadow-[0_16px_34px_rgba(23,22,21,0.18)] transition dark:bg-[#f7f2e7] dark:text-[#171615]";
const disabledClasses = "cursor-not-allowed opacity-60 hover:opacity-60";
---

{
  !Astro.props.availableForSale ? (
    <button disabled class={clsx(buttonClasses, disabledClasses)}>
      Out Of Stock
    </button>
  ) : !Astro.props.selectedVariantId ? (
    <button
      aria-label="Please select an option"
      disabled
      class={clsx(buttonClasses, disabledClasses)}
    >
      <div class="absolute left-0 ml-4">
        <PlusIcon class="h-5" />
      </div>
      Add To Cart
    </button>
  ) : (
    <button
      aria-label="Add to cart"
      class={clsx(buttonClasses, {
        "hover:-translate-y-0.5 hover:bg-[#4c5b48] dark:hover:bg-white": true,
      })}
      form="add-to-cart"
    >
      <div class="absolute left-0 ml-4">
        <PlusIcon class="h-5" />
      </div>
      Add To Cart
    </button>
  )
}
